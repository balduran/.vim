1. check out
  $ git clone git://github.com/balduran/dotvim.git ~/.vim

2. install
  $ sh ~/.vim/install.sh

3. add bundle
  $ git submodule add git://github.com/xxx/xxx.git bundle/xxx

4. init submodule
  $ git submodule update --init
